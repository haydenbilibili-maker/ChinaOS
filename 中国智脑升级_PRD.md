# 中国智脑（China Brain）深度升级 PRD

**版本**：v0.2  
**范围**：`china.html` 导航分组「中国智脑」下全部 Tab（全局仪表、HAL、Kernel、Telemetry、BIOS、Network、Event Log、Analysis）  
**设计原则**：与《中国深度调研系列_索引与恢复指令》术语与冷峻书面风格对齐；UI 延续玻璃拟态与权力红/钢灰/赛博青配色；叙事强调**超级复杂系统的分层架构**与**可审计的问题闭环**。

---

## 1. 产品目标

| 目标 | 说明 |
|------|------|
| G1 全局可观测 | 用户在「全局仪表」一眼看到系统总状态、区域脉冲、核心风险耦合与下钻入口。 |
| G2 问题驱动遥测 | 「遥测」以**问题清单/工单**为一等公民，与僵局压强、深度透视模块可跳转对齐。 |
| G3 持续追踪 | 「持续追踪日志」支持**级别 + 议题**过滤，与问题域、Network、Kernel 形成交叉引用。 |
| G4 分析与执行闭环 | 「重点问题分析」聚合雷达与路线图，并显式链路到数据总线与专题 Tab。 |

---

## 2. 用户与场景

- **分析型用户**：从全局仪表进入活地图或债务层，再回遥测看 P0 工单状态。  
- **复盘型用户**：在事件日志按「债务/房地产/就业」过滤，对照遥测同一问题域。  
- **演示型用户**：依赖架构总线与状态徽章快速讲清「国土基建—治理内核—问题遥测—网络 API」关系。

---

## 3. 信息架构（IA）

```
中国智脑
├── 全局仪表      → 总控台 + 活地图快捷层 + 智脑诊断快览
├── 国土空间与基建 (HAL)
├── 治理内核 (Kernel)
├── 问题清单遥测 (Telemetry)  → 信号仪表 + 问题工单 + 僵局监测
├── BIOS 固件
├── 交通通信网络 API (Network)
├── 持续追踪日志 (Event Log)  → 级别 / 议题双维过滤
└── 重点问题分析 (Analysis)
```

---

## 4. 功能需求（按优先级）

### P0 — 本期必交付

| ID | 需求 | 验收标准 |
|----|------|----------|
| P0-1 | **全局仪表 · 智脑诊断快览** | 展示僵局加权指数、阈值提示、P0 问题一句话摘要；提供跳转「问题清单遥测」「持续追踪日志」「活地图」的按钮。 |
| P0-2 | **遥测 · 问题工单表** | 至少 4 条工单（对应 P0–P3 域），含：编号、优先级、状态、主信号、与 `state.deadlockPressure` 或文案一致的关联说明、可点击跳转相关 Tab。 |
| P0-3 | **事件日志 · 议题过滤** | 日志行带 `data-topic`；提供议题 chips（全部/债务/房地产/就业/治理/改革/网络）；与原有 INFO/WARN/ERROR 过滤**同时生效**（交集）。 |
| P0-4 | **数据信任度滑块文案修复** | 拖动滑块时标签为可读中文，无 `??` 乱码。 |

### P1 — 紧随迭代

| ID | 需求 | 验收标准 |
|----|------|----------|
| P1-1 | **活地图省级下钻（可选数据源）** | 区域卡片或省份快捷按钮调用现有 `switchTab('map')` + `updateMapLayer`，并文档化限制（需地图 Tab 已初始化）。 |
| P1-2 | **Network ↔ HAL 映射说明** | 在 Network 页增加「物理层映射」短文或列表：交通/骨干网/卫星/算力与 HAL 国土空间叙事的对应关系。 |
| P1-3 | **Analysis 深度链接矩阵** | 为债务/地产/就业/改革四象限各提供至少一个 `switchTab` 到深度透视或数据 v2026 的入口。 |

### P2 — 体验与工程化

| ID | 需求 | 验收标准 |
|----|------|----------|
| P2-1 | 问题工单状态机 | 状态：`新建 / 跟踪中 / 缓和 / 复发`（可先静态，后续接本地 state）。 |
| P2-2 | 事件日志时间轴回放 | 按日聚合 + 播放条（可复用 BIOS 时间轴交互模式）。 |
| P2-3 | 导出「智脑一页纸」 | 复用现有 `exportReportAsHtml`，仅导出当前仪表+工单摘要区块。 |

### P3 — 长期

- 与真实外部 API（新闻/统计）对接的「热度」字段；工单与日志的持久化（localStorage / 后端）。

---

## 5. 非功能需求

- **性能**：不新增重型依赖；图表仍用现有 Chart.js / ECharts 初始化路径。  
- **可访问性**：新增按钮统一 `type="button"`；保留 `aria` 可读标签（逐步补全）。  
- **兼容**：保持 SPA `switchTab` / `initCharts(tabId)` 契约，避免重复绑定导致内存泄漏。

---

## 6. 里程碑建议

| 阶段 | 内容 |
|------|------|
| M1（当前） | P0 全部 + P1-2 文案映射（Network）+ P1-3 部分链接（Analysis） |
| M2 | P1-1 省级快捷 + P2-1 状态机 |
| M3 | P2-2 / P2-3 |

---

## 7. 变更记录

| 日期 | 说明 |
|------|------|
| 2026-04-02 | 初版 PRD；与代码实现 v0.2 对齐 |
| 2026-04-02 | **M1 落地**：P0-1～P0-4 已在 `china.html` 实现；P1-2（Network↔HAL 映射）、P1-3（Analysis 快捷入口）已实现；HAL 图表乱码标签已修复 |
