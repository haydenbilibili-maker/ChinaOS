# 能源主权模块 · China OS v2.0

React 单页应用，呈现能源转型与核电/风光/聚变/储能战略内容，与《中国深度调研系列》索引中的「能源压舱石」「基荷主权」「时空对冲」等逻辑锚点一致。

## 运行

```bash
cd energy-module
npm install
npm run dev
```

在浏览器打开终端提示的本地地址（通常为 http://localhost:5173）。

## 构建

```bash
npm run build
```

产物在 `dist/`，可部署到任意静态托管或挂到主站子路径。
