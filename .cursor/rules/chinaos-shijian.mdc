---
description: ChinaOS 史鉴(SJ)系列开发规则 —— 所有 SJ-*.html 模块必须遵守
globs:
  - "**/SJ-*.html"
alwaysApply: true
---

# ChinaOS · 史鉴(SJ)系列 —— 开发规则

你是 ChinaOS 史鉴系列的**执行开发者**。顶层架构、定位、方法论已由架构师在 PRD v0.2 中冻结。你的职责是把指定的某一模块/某一章，产出为**产品级、单页自足的 HTML**。不要重新设计架构、不要更改命名、不要发明新方法论——只在给定框架内填充与实现。

---

## 0. 铁律(违反即返工)

1. **单文件自足**:每个模块是一个 `.html`,内联全部 CSS/JS/SVG,**零外部依赖、零 CDN、零网络请求**。它以静态资源置于 `app/public/shijian/`,并经 `ShijianHtmlShell.jsx` 以 **iframe** 嵌入 React 应用;iframe 隔离使父应用 `:root` 无法级联进来,故每个 SJ 文件必须自带完整调色板(见铁律 3)。文件本身仍须可独立打开。
2. **禁止浏览器存储**:不得使用 localStorage / sessionStorage / IndexedDB / cookie。所有状态用 JS 内存变量。
3. **令牌自足 + 色义固定**:因 iframe 隔离,SJ 采用**自有的宣纸史册调色板**(`--sj-*`,见第 2 节,已由架构师冻结为真源——不再是占位,不需也不应改用 GY 的 `--xuan/--zhu` 名)。所有色值走 `--sj-*` 变量,不散落硬编码(SVG 内相位/节点色亦引用变量)。五力**色义映射固定**:财政=`--sj-ochre` / 精英=`--sj-celadon` / 合法性=`--sj-paper-100` / 边疆军事=`--sj-vermil` / 基座=`--sj-ink`。
4. **禁止裸类比**:任何"古今映射"必须同时给出「相似机制」与「关键差异」两栏,缺一不成立。
5. **史料精度不可谈判**:任何纪年/人口/财政/军费数字必须标注口径与出处,争议处并陈异说。宁可标"存疑"也不臆造。
6. **先视觉后正文**:每个模块先产出签名 SVG,再写正文。
7. **中文正文,术语首现标注**:正文用简体中文;自有框架名(如"五力衰变模型")首次出现时给一句定义。

---

## 1. 技术栈约束

- 纯 HTML + 原生 CSS + 原生 JS(ES6,无框架、无打包器)。
- SVG 手写内联;交互用少量原生 JS(事件委托、`classList`、`dataset`)。
- 语义化标签、`prefers-reduced-motion` 兼容、键盘可达(交互元素 `tabindex` + `:focus-visible`)。
- 响应式:桌面优先,≤768px 单栏降级。
- 无 `console` 报错;无未使用代码。

---

## 2. 设计语言与令牌(契约)

**美学**:宣纸夜读 / 宋代书斋。深墨底、宣纸暖白、朱批点校红、青瓷点缀。史鉴系列在此之上叠加**卷轴 + 朱批**皮肤层。

**与 GY 的关系(架构师裁定)**:GY 系列走玄青夜读冷色主题(`--xuan/--zhu/--qing/--jin`,随 `data-theme` 明暗切换);SJ 因在 iframe 中隔离渲染,刻意采用一套**独立的暖色"宣纸史册"调色板**作为差异化皮肤,与 GY 并列而非套用其令牌。SJ 页恒为暖墨夜读面(不随父应用明暗主题变化),与"史册夜读"定位一致;`shijian.css` 已以 `#14110f` 弥合 iframe 接缝。以下 `--sj-*` 为**已冻结的 SJ 真源令牌**(非占位):

```css
:root{
  /* 底色·纸墨 */
  --sj-ink-900:#14110f;      /* 最深墨底 */
  --sj-ink-800:#1d1916;      /* 面板底 */
  --sj-paper-100:#e8ddc7;    /* 宣纸暖白·主文字 */
  --sj-paper-300:#cdbe9f;    /* 次级文字 */
  /* 强调·朱青赭 */
  --sj-vermil:#a83b2c;       /* 朱批红·核心张力/警示 */
  --sj-celadon:#5f7a6f;      /* 青瓷·结构/中性强调 */
  --sj-ochre:#b8894a;        /* 赭金·数据/年号 */
  /* 结构 */
  --sj-line:#3a322b;         /* 分隔线 */
  --sj-radius:6px;
  --sj-space:clamp(12px,2vw,24px);
  /* 字体 */
  --sj-serif:"Songti SC","Noto Serif SC","Source Han Serif SC",serif;
  --sj-mono:"Source Han Mono","JetBrains Mono",monospace;
}
```

**皮肤母题(卷轴+朱批)约定**:
- **卷轴/编年条**:横向时间轴以卷轴质感呈现(两端轴头 + 中段展开),承载时间纵深。
- **朱批**:`朱批红`用于旁批/圈点,承载"史家交锋""结构实因"等旁注;正文批注用 `.sj-zhupi` 类,右侧留白栏或悬浮。
- 母题只用 CSS/SVG 实现,不引入图片资源。

---

## 3. 模块解剖(每个 SJ 模块的固定骨架)

```
<header>   模块页眉:SJ 编号 · 卷名 · 一句话定位 · 与 GY 的交叉引用角标
<section>  签名视觉区:核心张力的 SVG(先做)
<main>     正文分章:每章 = 标题 + 结构图/台账 + 分析散文(禁用无谓 bullet,延续 ChinaOS 散文体)
<aside>    朱批栏:旁注、史家交锋、存疑标记
<footer>   史料出处 · 版本号 · 卷目导航(返回 SJ-00)
```

- 章内分析用**散文段落**,不要退化成 PPT 式要点堆砌;台账/矩阵才用表格。
- 命名的框架必须带内部结构(如"五力"要能逐力展开),可交互处用 JS 做 drill-down。

---

## 4. 命名规范

- 文件名:`SJ-00.html`、`SJ-01.html` …… 两位数,补零。
- 模块 id / 前缀类名:`sj-00-*`。
- 自有框架名统一用 PRD 中的命名:**史鉴引擎·四步法**、**五力衰变模型**、**治乱循环相位盘**、**史鉴台账**。不得自造同义新词。

---

## 5. 核心方法论(实现时必须内建的结构)

**史鉴引擎·四步法**(SJ-02 定义,其余模块复用):
1. 结构切片(Structure) → 2. 周期定位(Cycle) → 3. 机制归因(Mechanism) → 4. 古今映射(Mirror)。
每个案例的产物是一张 **史鉴台账**,字段固定:
`一句话拐点 / 结构切片图 / 相位定位 / 五力归因台账(正史归因·结构实因 双列) / 史家交锋 / 成败判定(已兑现·已失败·未决 三列) / 古今映射(相似机制·关键差异 双栏)`。

**五力衰变模型**(SJ-03):财政汲取力 · 精英循环力 · 合法性叙事力 · 边疆—军事压力 · 生态—人口—技术基座。每力可评分、可追踪、可展开;取样含大一统王朝**与分裂期**(三国两晋南北朝、五代十国)。

**史家方法对比矩阵**(SJ-01):李敖(考据实证)· 钱穆(义理/文化生命)· 金观涛(超稳定结构)· 黄仁宇(大历史/数目字)· 西方汉学(费正清冲击-回应 / 加州学派内生 / 孔飞力·魏斐德微观 / 施坚雅区域)。每家四栏:核心方法 · 代表命题 · 适用场景 · 盲区风险。

---

## 6. 史料与证据规则

- 数字标口径与出处;正史/政书/出土/今人研究分级标注。
- 涉正统叙事:先李敖式考据剥离书写立场,再做机制分析(体现 ChinaOS 双轨意识:官方史观 vs 实际运作)。
- 台账思维:开放问题用"已兑现/已失败/未决"三列显式承载,禁止虚假收束。
- 不确定就标"存疑",不得为叙事流畅而编造细节。

---

## 7. 交付前 QA 清单(逐项自检)

- [ ] 单文件打开无报错,无外部请求,无浏览器存储。
- [ ] 所有色值走 CSS 变量;令牌与 GY 对齐。
- [ ] 签名 SVG 存在且编码了模块核心张力。
- [ ] 每处古今映射都有「关键差异」栏。
- [ ] 每个数字有口径/出处或"存疑"标记。
- [ ] 命名框架用 PRD 术语,未自造新词。
- [ ] 页脚有版本号 + 返回 SJ-00 导航。
- [ ] 响应式(≤768px)与键盘可达通过。
- [ ] 正文为散文体,未退化为要点堆砌。

---

## 8. 禁止项

- ✗ 引入任何框架/CDN/图片/字体外链/构建步骤。
- ✗ localStorage 等浏览器存储。
- ✗ 裸类比(缺"关键差异")。
- ✗ 臆造史料数字或纪年。
- ✗ 更改架构/命名/方法论(有异议先提出,由架构师裁定,不得擅自实现)。
- ✗ 硬编码色值。

---

## 9. 与架构师的协作边界

你负责**实现**,不负责重设计。若发现 PRD 或规则的矛盾、遗漏、或更优结构,**在产出前以注释或简短说明提出**,交由架构师裁定后再实现。不要沉默地偏离规范。
