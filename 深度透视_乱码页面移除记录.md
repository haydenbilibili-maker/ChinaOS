# 深度透视：导航与乱码治理说明

**最近更新**：已对「深度透视」导航做**结构性合并与去重**（同类项合并、无效/乱码专题从导航移除）；旧 `tab` 参数仍通过别名重定向到保留页面，避免书签失效。

---

## 一、为何撤销「自动批量隐藏」

此前曾用脚本对 `getXxxHTML()` **整段函数体**统计连续 `???` 子串，并将计数 >0 的专题从导航中移除。该做法存在问题：

- **误伤**：`???` 可能来自注释、历史占位或与可见正文无关的片段；部分页面主体仍可读却被整体隐藏。
- **目标偏差**：导航裁剪应服务「可读性与产品决策」，而非单一机械指标。

**结论**：不再以「整函数 ??? 计数」作为批量隐藏导航的默认策略。

---

## 二、新隐藏规则（置顶约定）

以下规则作为后续**若需**从 `appData.navGroups` →「深度透视」中**临时移除**某 Tab 时的依据；**默认不隐藏，全量展示。**

| 层级 | 条件 | 说明 |
|------|------|------|
| **H1 · 禁止自动** | 单独依赖脚本扫描 `???` 行数/次数 | 不得再作为批量下架导航的唯一依据。 |
| **H2 · 可隐藏（需人工确认）** | 用户可见区（首屏标题、段落、按钮文案）出现**连续乱码或明显不可读**，且**当轮迭代无法修复** | 可将该 `{ id, label }` 从 `subCategories` 中临时删除，并在下表「当前隐藏列表」登记。 |
| **H3 · 替代入口** | 专题已有独立高质量页面（如 `straits.html`、`grain-security.html`、`power-logic.html`） | 优先在「报告枢纽」或正文内给外链；**不一定**从深度透视移除，除非产品明确要求 SPA 内仅保留内嵌或仅保留外链之一。 |
| **H4 · 流程** | 任意隐藏 | 记录在本文档「当前隐藏列表」→ 改 `china.html` 导航 → PR/提交说明原因与计划恢复时间或修复 Issue。 |

---

## 三、当前隐藏列表

以下为**从主导航移除**或**仅保留别名**的条目（代码中 `case` 仍可能保留以兼容旧链接）：

| tab id | 处理 | 说明 |
|--------|------|------|
| `diplomacy` | 导航移除；别名 → `diplomacyGlobal` | 原「外交」页可见区乱码多，统一走全球外交视图。 |
| `resources` | 导航移除；别名 → `resourcesOverseas` | 原「资源」页与海外资源专题重复，保留海外战略资源入口。 |
| `guochao` | 导航移除；别名 → `culture` | 「国潮」专题无效/乱码为主，合并至文化页。 |
| 若干同类项 | 仅导航合并 | 如汽车/汽车主权→`automotiveSovereignty`；能源/能源主权→`energySovereignty`；数字化/数据要素/数字经济→`digitalEconomy`；乡村/乡村振兴→`ruralRevitalization`；城镇化/新型城镇化→`newUrbanization`；生态/生态文明→`ecologicalCivilization`；半导体/芯片主权→`semiconductor`；航天相关多 Tab→`space`；民营/民营经济→`privateEconomy` 等。 |

若以后有**临时**隐藏其它 Tab，在此逐条追加：`tab id`、原因、责任人/日期、恢复条件。

---

## 四、历史记录（仅供参考）

以下为旧版「脚本扫描 + 批量隐藏」时期曾从导航移除的 Tab 范围备忘，**不代表当前导航状态**。修复对应 `get*HTML` 正文乱码后，无需再经「脚本清零 ???」即可继续在导航中保留。

- 涉及原移除条目的分类：制度与改革（含权力逻辑、法治）、社会与民生（医疗/社会治理/住房/教育）、产业与制造多数条目、科技与创新多数条目、区域与开放多数条目、金融部分条目、安全与国防部分条目等。
- 独立报告替代：`grain-security.html`、`straits.html`、`political-decision-logic.html` 等与内嵌 `get*HTML` 可并存，由产品决定主推方式。

---

## 五、与 `china.html` 的对应关系

`china.html` 内 `appData.navGroups` 中 `label: '深度透视 (Depth)'` 的 `subCategories` 为**唯一导航数据源**；注释中指向本文件，用于说明「全量展示 + 新隐藏规则」。

---

*文档用途：治理深度透视导航与乱码修复流程；与「中国深度调研系列」索引配合使用。*
